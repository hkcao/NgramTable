[project]
name = "ngram-hash-speculative"
version = "0.1.0"
description = "Hash-table based NGram speculative decoding for vLLM"
requires-python = ">=3.10"
dependencies = [
    "vllm>=0.8.0",
    "datasets",
    "torch",
    "numpy",
    "numba",
]
